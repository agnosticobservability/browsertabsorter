## 2024-05-22 - URL Parsing Bottleneck **Learning:** Repeated `new URL(url)` calls in loops (e.g., grouping/sorting strategies) can be expensive (5-10µs per call), especially when processing hundreds of tabs multiple times. Simple caching can yield ~100x speedup for repetitive URL parsing. **Action:** Implement simple memoization for pure functions that process strings, especially in hot paths like tab processing loops.
