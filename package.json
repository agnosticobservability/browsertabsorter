{
  "name": "browsertabsorter",
  "version": "1.0.0",
  "description": "Chromium MV3 extension for grouping and sorting tabs",
  "license": "MIT",
  "scripts": {
    "prebuild": "node scripts/generate-icons.mjs",
    "build": "npm run type-check && node scripts/build.mjs",
    "type-check": "tsc --noEmit",
    "postinstall": "npm run build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "@types/node": "^25.0.8",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "p-limit": "^7.3.0"
  }
}
